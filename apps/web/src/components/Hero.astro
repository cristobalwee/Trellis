---
import { FloralDecoration } from './FloralDecoration';
import { AnimatedCTA } from './AnimatedCTA';
---

<section class="relative min-h-screen flex items-center justify-center overflow-x-hidden">
  <FloralDecoration client:load />
  <div class="container-custom relative z-10">
    <!-- Main Content -->
    <div class="max-w-4xl lg:max-w-5xl xl:max-w-5xl 2xl:max-w-6xl mx-auto text-center mt-[-100px]" data-scroll data-scroll-speed="-0.1">
      <!-- Headline -->
      <h1
        class="hero-headline mb-6 md:mb-8"
        data-animate="headline"
      >
        Generate a complete design system <i>in 10 minutes</i>
      </h1>

      <!-- Sub-headline -->
      <p
        class="hero-subheadline text-base md:text-lg lg:text-xl leading-tight text-charcoal/80 mb-10 md:mb-12 max-w-3xl mx-auto"
        data-animate="subheadline"
      >
      Color ramps, tokens, react components, Figma library, and foundational documentation, all customized to your brand. Skip the months-long setup and start building today.
      </p>

      <!-- CTA Button -->
      <div data-animate="cta" class="flex flex-col items-center gap-4">
        <AnimatedCTA
          client:load
          id="hero-cta"
          variant="primary"
          size="lg"
        >
          Start Building
        </AnimatedCTA>
        <p class="text-charcoal/60 text-sm">$99 one-time purchase</p>
      </div>
    </div>
  </div>

</section>

<style>
  /* Typography Animations */
  [data-animate='headline'] {
    opacity: 0;
    animation: fadeInUp 1.6s cubic-bezier(0.17,0.84,0.44,1) forwards;
    animation-delay: 0.4s;
  }

  [data-animate='subheadline'] {
    opacity: 0;
    animation: fadeInUp 1.6s cubic-bezier(0.17,0.84,0.44,1) forwards;
    animation-delay: 0.55s;
  }

  [data-animate='cta'] {
    opacity: 0;
    animation: fadeInUp 1.6s cubic-bezier(0.17,0.84,0.44,1) forwards;
    animation-delay: 0.7s;
  }
</style>

<script>
  // Connect Hero CTA to BrandIntake modal
  const heroCTA = document.getElementById('hero-cta');

  heroCTA?.addEventListener('click', () => {
    // Dispatch custom event to open BrandIntake modal
    window.dispatchEvent(new CustomEvent('openBrandIntake'));
  });
</script>
